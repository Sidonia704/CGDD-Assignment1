{
  "name": "last-dash",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20.19.0"
  },
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "prepare:models": "node scripts/prepare-models.mjs",
    "build": "npm run prepare:models && vite build && node scripts/publish.mjs",
    "test": "node --test tests/combat.test.mjs",
    "preview": "vite preview --host 127.0.0.1"
  },
  "dependencies": {
    "three": "0.185.1"
  },
  "devDependencies": {
    "vite": "8.2.2"
  }
}
